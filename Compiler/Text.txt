//#define A

#define G I
#define I(x) #x

#define H(x) (x)x

H(G) 6 (i)

#define A B
#define B C
#define C D

#define S1(x) S2(x)
#define S2(x) S3(x)
#define S3(x) # x

S2(A) S3(A)
/*
#ifdef A
spravne
#else
chybne
#endif

#ifndef A
chybne

#include "what.h"
ignored

#else

# ifdef BB
chybne
# endif

spravne
#endif

#undef A

#ifndef A
spravne
#else
chybne
#endif

#ifdef A
chybne
#else
spravne
#endif
*/