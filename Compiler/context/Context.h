#pragma once

#include "../common/Position.h"

namespace compiler {
    namespace context {

        common::Position position();
        void position( common::Position * );

    } // namespace context
} // namespace compiler
